version: 2
updates:
  # Maven 依赖自动升级（每周日检查，PR 目标分支 dependa）
  - package-ecosystem: maven
    directory: "/"
    target-branch: dependa
    schedule:
      interval: weekly
      day: sunday
      time: '02:00'
      timezone: Asia/Shanghai
    open-pull-requests-limit: 10
    labels:
      - dependencies

  # GitHub Actions 依赖自动升级（每周日检查，PR 目标分支 dependa）
  - package-ecosystem: github-actions
    directory: "/"
    target-branch: dependa
    schedule:
      interval: weekly
      day: sunday
      time: '02:00'
      timezone: Asia/Shanghai
    open-pull-requests-limit: 10
    labels:
      - dependencies
